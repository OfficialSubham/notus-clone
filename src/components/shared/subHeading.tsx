const SubHeading = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="text-sm text-neutral-400 text-center max-w-lg">
      {children}
    </div>
  );
};

export default SubHeading;
